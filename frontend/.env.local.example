# Origin of the FastAPI backend. Next.js rewrites /api/* to it (see
# next.config.mjs), so the browser stays same-origin with the API.
BACKEND_ORIGIN=http://localhost:8000

# Show the "Đăng nhập với Microsoft" button (backend SSO_ENABLED must also be true).
NEXT_PUBLIC_SSO_ENABLED=false
